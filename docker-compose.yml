version: '3.9'
services:
  api:
    build: ./apps/api
    ports:
      - '8080:8080'
    environment:
      - PORT=8080

  web:
    build:
      context: ./apps/web
      args:
        VITE_API_BASE_URL: http://localhost:8080/api
    ports:
      - '5173:8080'
    depends_on:
      - api
